import React from 'react';
import type { NextPage } from 'next';
import Head from 'next/head';

const Home: NextPage = () => {
  return (
    <section>
      <Head>
        <title>Login</title>
      </Head>
    </section>
  );
};

export default Home;
