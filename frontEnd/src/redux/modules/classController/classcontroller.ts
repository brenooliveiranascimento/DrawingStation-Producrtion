const CLASSROOM_CONTROLLER_DEFAULT_VALUE = {
  classroom: {
    name: '',
    premium: true,
    video: '',
    description: '',
    colors: null,
    image: '',
  }
};

export function classroomController(state: any, action: any) {
  switch(action.type) {
  default:
    return state;
  }
}